The below code is what is needed to get consistent fonts and that green bar at the top.
There is a way to create a style.css file so each of the html pages we write could reference this 
and we would have to write repeat code, but i couldnt get it to work. 
so just pasting this here to copy as needed.

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Page</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #81C784; /* Green */
            --secondary-color: #3D405B; /* Darker color for buttons */
        }

        body {
            margin: 0;
            font-family: 'Outfit', sans-serif;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: var(--primary-color);
            color: white;
            padding: 10px 20px;
        }

        .summary-button {
            font-family: 'Outfit', sans-serif;
            font-weight: 600;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border: none;
            border-radius: 5px;
            background-color: var(--secondary-color);
            color: white;
        }

        .summary-button:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>
    <!-- Reusable Header -->
    <header>
        <h1>Ingredient Tracker</h1>
        <button class="summary-button" onclick="openStats()">Summary Statistics</button>
    </header>

    <main>
        <p>Welcome to the new page!</p>
    </main>

    <script>
        // Define the function for the button
        function openStats() {
            alert('Summary Statistics button clicked!');
        }
    </script>
</body>
</html>
